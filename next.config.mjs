/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["api.escuelajs.co", "placehold.co", "i.imgur.com", "i.ibb.co"],
  },
};

export default nextConfig;
